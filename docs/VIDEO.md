# Video plan — three minutes

Record student half at **360px**; operator consoles at desktop. No slides. No music bed over speech. Hindi-first voice-over is fine; English subtitles optional for judges.

| Time | Beat | What to show |
|---|---|---|
| 0:00–0:20 | The problem | Landing claims: eight doors, three-month stall (GOVUP/E/2026/0035742), silence. Banner visible. |
| 0:20–0:40 | One door | `/pravesh` OTP (on-screen mock) → `/otr` with demo Aadhaar → mint twice → duplicate recovers, does not debar → `/raasta` with “पता नहीं” → renewal safely |
| 0:40–1:05 | Pre-flight | Open case → `/taiyari` → verify expired income cert → blocker naming both dates → fix to valid cert → blockers clear |
| 1:05–1:30 | One form | `/aavedan` — prefilled renewal note if any; fee panel struck-through heads; type marks; watch save chip; (optional) kill network briefly → “इस फोन पर सेव” |
| 1:30–1:50 | Lock | `/jaanch` consequences + hard-copy clock → lock → case file with registration number |
| 1:50–2:20 | Institute + clock | `/sansthan` PIN 1234 → refuse forward without hard copy → attendance 68 refused → 86 forward → `/mock` +12/+20 days → case file breach, escalation, grievance draft, wait counter unchanged |
| 2:20–2:45 | DWO | `/dwo` → cross-check side-by-side → flag `ENROLMENT_MISMATCH` → student file becomes task list with fix → verify another path / correction → sanction |
| 2:45–3:00 | Payment + close | `/mock` PFMS → bounce `NPCI_NOT_SEEDED` with bank action → requeue/force credited → paid → cut to `/seemayein` |

## Shot list checklist

- [ ] Banner readable in first frame
- [ ] Demo Aadhaar `0000…` stated aloud
- [ ] Public `/t/<id>` opened in a second profile (no login)
- [ ] Simulator chip “मॉक” visible when NPCI/e-District taken down
- [ ] End card: repo URL + live demo URL + “independent prototype”

## Audio notes

Name the invariant once: “हर चरण का एक नाम वाला ज़िम्मेदार और एक तारीख़.” Close on what software cannot fix (staffing, bank seeding, budget timing) so honesty is audible.
